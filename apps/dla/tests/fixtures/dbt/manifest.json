{
  "metadata": {
    "dbt_schema_version": "https://schemas.getdbt.com/dbt/manifest/v12.json",
    "project_name": "demo"
  },
  "nodes": {
    "model.demo.orders": {
      "resource_type": "model",
      "name": "orders",
      "schema": "public",
      "description": "Customer orders fact table, one row per placed order (per dbt docs).",
      "columns": {
        "status": {
          "name": "status",
          "description": "Order fulfilment state maintained by the order-service."
        },
        "total_amount": {
          "name": "total_amount",
          "description": "Order grand total in USD, tax inclusive."
        }
      }
    },
    "seed.demo.raw_lookup": {
      "resource_type": "seed",
      "name": "raw_lookup",
      "schema": "public",
      "description": "A seed node that should be ignored by the importer."
    }
  }
}
